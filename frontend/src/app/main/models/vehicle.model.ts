export interface Vehicle {
    id: string;
    vehicleCode: number;
    vehiclePlate: string;
    vehicleCard: number;
    trailerPlate: string;
    vehiclePhoneNumber: string;
    hidden?: boolean;
}
